namespace MediQueue.Domain.Tests;

/// <summary>
/// Placeholder so that <c>dotnet test</c> and the CI step have something to run
/// from the first commit. Replaced by the real domain suite — the state machine
/// transition table, the TAJ and patient-name rules, and the assignment
/// strategy — in P1.
/// </summary>
public class SolutionSmokeTests
{
    [Fact]
    public void Solution_builds_and_tests_run()
    {
        true.ShouldBeTrue();
    }
}
